@model IEnumerable<L01P02_2020MV603_2020DO601.Models.motorista>

@{
    ViewData["Title"] = "Index";
}

<h1>MOTORISTAS</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nombreMotorista)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombreMotorista)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.motoristaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.motoristaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.motoristaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
